<div class="tab-content tabs-content">
    {% for tab in tabs %}
        <div
                class="tab-pane fade {{ tab.active ? 'show active' }}"
                id="{{ tab.id }}"
                role="tabpanel"
        >
            {% block content %}
            {% endblock %}
        </div>
    {% endfor %}
</div>